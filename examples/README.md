# examplecpplib/examples

Each subdirectory represents an example application (or library) that uses our `mylib` library.

It's always nice to ship a library's source code with a working example or two.
